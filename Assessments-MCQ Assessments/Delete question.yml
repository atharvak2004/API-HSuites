info:
  name: Delete question
  type: http
  seq: 11
  tags:
    - Assessments/MCQ Assessments

http:
  method: DELETE
  url: "{{baseUrl}}assessments/mcq-questions/100/delete/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}assessments/mcq-questions/100/delete/"
      method: DELETE
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 404 Response
    description: Failure
    request:
      url: "{{baseUrl}}assessments/mcq-questions/100/delete/"
      method: DELETE
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
