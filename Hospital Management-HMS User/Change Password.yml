info:
  name: Change Password
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}hospital/hmsuser/change-password/4/"
  body:
    type: json
    data: |-
      {
          "old_password": "y9cPhsIgU7",
          "new_password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
